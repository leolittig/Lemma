# Instructions for Task Nodes (`type: task`)

- **`task` → its activity.** A task links to the activity it serves (`[[University]]`, `[[3DPrinting]]`). A task with no clear parent may link to the root.
- **Frontmatter on a `task`**: include `status: open` or `status: done`, and `tags: tag1, tag2`.
