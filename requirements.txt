# Web server
fastapi
uvicorn
pydantic            # request/response schemas (also pulled in by fastapi)
python-multipart    # required by FastAPI for file uploads (the /upload endpoint)

# Hugging Face model downloads
huggingface-hub

# Inference engines (an engine is chosen per model from its format)
# MLX — Apple Silicon only; the default engine on macOS. Supports text, image
# and audio input. mlx-vlm>=0.6.3 adds the diffusion_gemma architecture.
mlx; sys_platform == 'darwin'
mlx-vlm>=0.6.3; sys_platform == 'darwin'
# llama.cpp — runs GGUF models everywhere; the default engine off macOS.
llama-cpp-python
