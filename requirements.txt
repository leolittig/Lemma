fastapi
uvicorn
huggingface-hub
aiofiles
# MLX is Apple-Silicon only; it's the default engine on macOS.
mlx; sys_platform == 'darwin'
# 0.6.3 adds the diffusion_gemma architecture (DiffusionGemma block-diffusion
# models); earlier versions fail to load them.
mlx-vlm>=0.6.3; sys_platform == 'darwin'
# llama.cpp runs GGUF models everywhere (the default engine off macOS).
llama-cpp-python
